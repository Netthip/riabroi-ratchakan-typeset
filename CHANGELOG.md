# บันทึกการเปลี่ยนแปลง · Changelog

## v1.2.0 — 21 ก.ย. 2569

ใช้บน macOS ได้ — ส่วนตัดคำและจัดหน้าเหมือนเดิมทุกอย่าง ที่เพิ่มคือการส่งออก PDF เพื่อตรวจผลบน Mac
และตัวตรวจที่จับปัญหาซึ่งเจอบ่อยบน Mac (ฟอนต์ไม่มี · ชื่อฟอนต์ซ้อน)

**ส่งออก PDF (`inspect_docx.py`)**
- `to_pdf` เลือกตัวส่งออกตามเครื่อง: Windows ใช้ Word ผ่าน COM เหมือนเดิม · macOS สั่ง Word for Mac ผ่าน
  AppleScript (`scripts/word_pdf_mac.applescript`) · ไม่มี Word ใช้ LibreOffice ได้พร้อมคำเตือน
  (`--engine libreoffice`) · `engine="word"` บังคับว่าต้องเป็น Word
- Word for Mac: เปิดสำเนาชื่อสุ่มใน `~/riabroi-pdf` ปิดเฉพาะไฟล์นั้น ไม่ปิด Word ที่ผู้ใช้เปิดอยู่ ·
  แปลงรหัสข้อผิดพลาด (สิทธิ์ Automation, กล่อง Grant File Access ค้าง) เป็นวิธีแก้ภาษาไทย
- Windows: ให้ Word ส่งออกลงเทมป์ก่อนแล้ว Python ค่อยคัดลอกไปปลายทาง — เดิมเขียนตรงไปโฟลเดอร์ที่ส่งมา
- รายงานบอกว่า PDF มาจากโปรแกรมไหน และ **ตรวจฟอนต์ถูกแทน** (`font_check`) — ไม่มีฟอนต์ Word จะใช้ตัวอื่นแทน
  เงียบ ๆ แล้วผลวัดใช้ไม่ได้
- แก้อักษรเพี้ยนตอนดึงข้อความจาก PDF (รหัส PUA ของ TH SarabunPSK · สระอำแตก) ก่อนตรวจตัดกลางคำ

**ใหม่: `scripts/doctor.py`** ตรวจเครื่องก่อนใช้ — Python ไลบรารี ตัวตัดคำ ฟอนต์ Word/LibreOffice
- `--pdf` ส่งออกหน้าทดสอบจริงแล้ววัดว่า Word ตัดบรรทัดตรง ZWSP/ช่องว่างทุกบรรทัดไหม
  (เทียบกับต้นฉบับตรง ๆ ไม่ใช้พจนานุกรม จึงไม่มีผลบวกลวงแบบ "ดำเนินการ | ตาม")
- อ่านชื่อฟอนต์จากตาราง name เอง — ไฟล์ TH SarabunIT๙ รุ่น Windows บอก Mac ว่าชื่อตระกูลคือ TH SarabunPSK
  และวาดเลขอารบิกเป็นรูปเลขไทย บน Mac จึงเตือนเมื่อเจอไฟล์แบบนี้

**ติดตั้ง**
- `pywin32` ติดตั้งเฉพาะ Windows — เดิม `pip install -r requirements.txt` บน Mac ล้มทั้งไฟล์
- `.gitattributes` ให้ไฟล์ AppleScript และ shell script เป็น LF · `.gitignore` เพิ่ม `.venv/`

**เอกสาร** README (ไทย/อังกฤษ) หัวข้อใช้บน Mac · SKILL.md หัวข้อเครื่อง Mac และข้อตรวจฟอนต์ในรายการก่อนส่ง ·
`references/toolchain.md` หัวข้อ 3 Word for Mac

เทสต์ 25 → 39 ข้อ · ตรวจซ้ำบน Windows: ตัวอย่างประเด็นถามตอบ 4 ไฟล์ได้หน้าเท่าเดิมและรอยต่อบรรทัดตรงกับ
ฉบับที่ตรวจด้วยตาแล้วทุกจุด · **ทางเดิน Mac ผ่านเทสต์แบบจำลอง แต่ยังไม่ได้ลองกับ Word for Mac จริง**

## v1.1.0 — 21 ก.ย. 2569

ทำตัวอย่างเอกสารประเด็นถามตอบ กมธ./วุฒิสภา ที่ถูกต้อง แล้วพบว่า v1.0 ยังไม่ตรงกับวิธีจริงของกิ๊ฟ
รุ่นนี้แก้ตามที่เจอ และทดสอบกับเอกสารชุดเดียวกัน ผลตรงกับตัวอย่างที่ตรวจด้วยตาแล้ว
(หน้าเท่ากัน · ตัดกลางคำ 0 · ไม่มีบรรทัดถ่าง)

**ตัดคำ (`thai_break.py`)**
- เพิ่มชั้น `keep_together` ถอดจากเครื่องกำเนิดใบเสนองาน (โม 09): "ความ"/"การ" ติดคำถัดไป ·
  เลขกับหน่วยนับ · จำนวนเงินกับ "บาท" · พ.ศ.กับปี · วงเล็บสั้น · เลขหนังสือ · คำนำวลีมัดด้วย NBSP
- คำนำวลี **ไม่มัดข้ามคำเชื่อม** แล้ว — v1.0 มัดเป็นทอดจน "ให้เต็มวงเงิน / เมื่อ…" ติดกันทั้งก้อน
- กฎใหม่จากการอ่านรอยต่อบรรทัดทีละจุด: ชื่อย่อ (สวรส. ม.มหิดล) · คำนำประสม (ให้มี อย่างมี เพื่อให้) ·
  "เชิง"/"ทาง" ติดคำถัดไป
- `DICT_GAPS` คำที่พจนานุกรมไม่มี (เฝ้าระวัง กรมควบคุมโรค รวมถึง …) และ `WHOLE_COMPOUNDS`
  คำประสมที่ซอยแล้วความหมายเพี้ยน (โทรศัพท์มือถือ สิทธิประโยชน์ จัดซื้อจัดจ้าง)
- แก้บั๊กที่ติดมาจากโม 09: รูปแบบวลีกิน ZWSP ที่อยู่หลังวลีไปด้วย จุดตัดหลังวลีจึงหายเสมอ
- `insert_zwsp_document` ใส่ ZWSP **เฉพาะย่อหน้าที่จัดชิดขอบขวา** เป็นค่าตั้งต้น
  (บทเรียนโม 09 ก.ค. 2569: ใส่ทุกย่อหน้าแล้ว "แยกถูกผิดไม่ออก") — ไม่มีย่อหน้าไหนชิดขอบขวาจะเตือน
- เพิ่ม `plain_text()` ไว้เทียบว่าข้อความไม่เปลี่ยน (ถอด ZWSP และคืน NBSP เป็นช่องว่าง)

**จัดหน้า (`thai_fit.py`)**
- เพิ่ม `justify_body()` จัดเนื้อความชิดขอบขวา ไม่แตะหัวเรื่อง ป้ายสั้น บล็อกลายเซ็นที่ใช้ tab

**ตรวจผล (`inspect_docx.py`)**
- เพิ่ม `line_breaks()` และ `--breaks` แสดงรอยต่อบรรทัดทุกจุดไว้อ่านเอง — ตัวตรวจอัตโนมัติใช้
  พจนานุกรมเดียวกับตัวตัดคำ มองไม่เห็นคำที่พจนานุกรมไม่มี
- แก้ `to_pdf` พังเมื่อส่งโฟลเดอร์แบบ relative (Word ต้องการ path เต็ม)

**เอกสาร**
- `references/forms.md` แก้ที่เขียนผิดว่าเอกสารประเด็นถามตอบ "ชิดซ้าย ไม่ใช้ thaiDistribute"
- `references/typesetting.md` เพิ่มหัวข้อ 8 รวมกฎ keep_together ทั้งหมดและวิธีเพิ่มคำ
- SKILL.md เพิ่มขั้น "อ่านรอยต่อบรรทัดทุกจุดด้วยตา" ในรายการตรวจก่อนส่ง

เทสต์ 10 → 25 ข้อ

## v1.0.0 — 21 ก.ย. 2569

รุ่นแรก — สกิลตัดคำและจัดตัวหนังสือเอกสารราชการไทย แยกออกจาก thai-docx
- ตัดคำจากทั้งย่อหน้าแล้วแมปกลับเข้า run · สระหน้าตัดข้างหน้าได้ · ซอยคำประสมยาวตามพจนานุกรม
- ฟอนต์ฝั่ง complex script ครบ · เรียง `w:rPr` ตาม schema · บีบอักษรเลือกขั้นที่ถ่างน้อยสุด
- ตัวตรวจผลจาก PDF · เทสต์ 10 ข้อ · ทดสอบการหยิบสกิล 14/14
