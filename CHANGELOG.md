# บันทึกการเปลี่ยนแปลง · Changelog

## v1.1.0 — 21 ก.ย. 2569

ทำตัวอย่างเอกสารประเด็นถามตอบ กมธ./วุฒิสภา ที่ถูกต้อง แล้วพบว่า v1.0 ยังไม่ตรงกับวิธีจริงของกิ๊ฟ
รุ่นนี้แก้ตามที่เจอ และทดสอบกับเอกสารชุดเดียวกัน ผลตรงกับตัวอย่างที่ตรวจด้วยตาแล้ว
(หน้าเท่ากัน · ตัดกลางคำ 0 · ไม่มีบรรทัดถ่าง)

**ตัดคำ (`thai_break.py`)**
- เพิ่มชั้น `keep_together` ถอดจากเครื่องกำเนิดใบเสนองาน (โม 09): "ความ"/"การ" ติดคำถัดไป ·
  เลขกับหน่วยนับ · จำนวนเงินกับ "บาท" · พ.ศ.กับปี · วงเล็บสั้น · เลขหนังสือ · คำนำวลีมัดด้วย NBSP
- คำนำวลี **ไม่มัดข้ามคำเชื่อม** แล้ว — v1.0 มัดเป็นทอดจน "ให้เต็มวงเงิน / เมื่อ…" ติดกันทั้งก้อน
- กฎใหม่จากการอ่านรอยต่อบรรทัดทีละจุด: ชื่อย่อ (สวรส. ม.มหิดล) · คำนำประสม (ให้มี อย่างมี เพื่อให้) ·
  "เชิง"/"ทาง" ติดคำถัดไป
- `DICT_GAPS` คำที่พจนานุกรมไม่มี (เฝ้าระวัง กรมควบคุมโรค รวมถึง …) และ `WHOLE_COMPOUNDS`
  คำประสมที่ซอยแล้วความหมายเพี้ยน (โทรศัพท์มือถือ สิทธิประโยชน์ จัดซื้อจัดจ้าง)
- แก้บั๊กที่ติดมาจากโม 09: รูปแบบวลีกิน ZWSP ที่อยู่หลังวลีไปด้วย จุดตัดหลังวลีจึงหายเสมอ
- `insert_zwsp_document` ใส่ ZWSP **เฉพาะย่อหน้าที่จัดชิดขอบขวา** เป็นค่าตั้งต้น
  (บทเรียนโม 09 ก.ค. 2569: ใส่ทุกย่อหน้าแล้ว "แยกถูกผิดไม่ออก") — ไม่มีย่อหน้าไหนชิดขอบขวาจะเตือน
- เพิ่ม `plain_text()` ไว้เทียบว่าข้อความไม่เปลี่ยน (ถอด ZWSP และคืน NBSP เป็นช่องว่าง)

**จัดหน้า (`thai_fit.py`)**
- เพิ่ม `justify_body()` จัดเนื้อความชิดขอบขวา ไม่แตะหัวเรื่อง ป้ายสั้น บล็อกลายเซ็นที่ใช้ tab

**ตรวจผล (`inspect_docx.py`)**
- เพิ่ม `line_breaks()` และ `--breaks` แสดงรอยต่อบรรทัดทุกจุดไว้อ่านเอง — ตัวตรวจอัตโนมัติใช้
  พจนานุกรมเดียวกับตัวตัดคำ มองไม่เห็นคำที่พจนานุกรมไม่มี
- แก้ `to_pdf` พังเมื่อส่งโฟลเดอร์แบบ relative (Word ต้องการ path เต็ม)

**เอกสาร**
- `references/forms.md` แก้ที่เขียนผิดว่าเอกสารประเด็นถามตอบ "ชิดซ้าย ไม่ใช้ thaiDistribute"
- `references/typesetting.md` เพิ่มหัวข้อ 8 รวมกฎ keep_together ทั้งหมดและวิธีเพิ่มคำ
- SKILL.md เพิ่มขั้น "อ่านรอยต่อบรรทัดทุกจุดด้วยตา" ในรายการตรวจก่อนส่ง

เทสต์ 10 → 25 ข้อ

## v1.0.0 — 21 ก.ย. 2569

รุ่นแรก — สกิลตัดคำและจัดตัวหนังสือเอกสารราชการไทย แยกออกจาก thai-docx
- ตัดคำจากทั้งย่อหน้าแล้วแมปกลับเข้า run · สระหน้าตัดข้างหน้าได้ · ซอยคำประสมยาวตามพจนานุกรม
- ฟอนต์ฝั่ง complex script ครบ · เรียง `w:rPr` ตาม schema · บีบอักษรเลือกขั้นที่ถ่างน้อยสุด
- ตัวตรวจผลจาก PDF · เทสต์ 10 ข้อ · ทดสอบการหยิบสกิล 14/14
