---
name: riabroi-ratchakan-typeset
description: |
  วิธีทำเอกสารราชการไทยของกิ๊ฟ (นักวิเคราะห์งบประมาณ สำนักงบประมาณ) — กฎจัดหน้าที่กิ๊ฟใช้จริงและเคยแก้มือมาแล้ว
  ค่าขอบกระดาษ/ขนาดอักษรที่วัดจากแม่แบบจริง วิธีสร้างไฟล์ที่ไม่พัง และกฎเหล็กเรื่องตัวเลขต้นทาง
  ใช้ skill นี้ทุกครั้งที่ต้องสร้าง แก้ จัดหน้า หรือตรวจเอกสารของกิ๊ฟ: ใบเสนองาน กระดาษร่าง
  ใบเห็นชอบแผนฯ ใบอนุมัติเงินจัดสรร หนังสือราชการ บันทึกข้อความ ประเด็นถามตอบ กมธ./วุฒิสภา
  เอกสารแนบ รายงานเสนอผู้บริหาร หรือไฟล์ .docx ภาษาไทยใด ๆ ในงานสำนักงบประมาณ
  Trigger เมื่อ: "จัดหน้าให้สละสลวย", "คำตกบรรทัดแบ่งคำไม่ดี", "บรรทัดถ่าง", "ขอบขวาไม่ชิด",
  "ทำใบเสนองาน", "ทำหนังสือราชการ", "แก้เอกสารให้หน่อย", "เทียบแม่แบบ", "สร้าง docx ไทย",
  หรือชื่อไฟล์/โฟลเดอร์งานงบประมาณของกิ๊ฟโผล่มาในคำสั่ง
  ใช้ skill นี้แทน thai-docx ทั่วไปเสมอเมื่อเป็นงานของกิ๊ฟ เพราะ thai-docx เป็นกฎกลาง
  ส่วนนี่คือกฎบ้านที่กิ๊ฟตัดสินไว้แล้วและเคยทักเมื่อทำไม่ครบ
---

# เรียบร้อย (riabroi) — เอกสารราชการสไตล์กิ๊ฟ

กิ๊ฟบอกไว้ตรง ๆ (6 ก.ย. 2569): **"นี่คืองานของเรา"** — เรื่องจัดหน้าไทยไม่ต้องรอให้กิ๊ฟทัก
ต้องไล่ให้ครบเองทุกครั้งที่ออกเอกสาร ไม่ใช่ให้กิ๊ฟคอยบอกกฎซ้ำ ๆ

เอกสารพวกนี้ส่งผู้บริหารลงนาม ตัดคำกลางคำ ขนาดอักษรไม่ตรงแม่แบบ หรือช่องไฟถ่าง
ทำให้ทั้งฉบับดูไม่เรียบร้อย แล้วกิ๊ฟต้องมานั่งแก้มือทีละหน้า — นั่นคือสิ่งที่ skill นี้กันไว้

## ลำดับงาน

1. **เทียบแม่แบบก่อนเขียนโค้ดบรรทัดแรก** — เปิดต้นฉบับ/ฉบับปีก่อนแล้ววัดจริง
   ห้ามใช้ค่าปริยายของตัวสร้าง (เคยผิดทั้งขอบบนและขอบข้าง กิ๊ฟทักว่า "ขอบบนสูงเกินไป ขอบข้างด้วย")
2. **สร้างไฟล์ในโฟลเดอร์ชั่วคราว** แล้วค่อยคัดลอกไปปลายทาง — ห้ามให้ Word (COM บน Windows หรือ
   AppleScript บน Mac) เขียนลง OneDrive ตรง ๆ
3. **ใส่ ZWSP ตั้งแต่ตอนสร้าง** ไม่ใช่ไปตามแก้ทีหลัง
4. **จัดหน้า** ตามกฎใน `references/typesetting.md` — บีบอักษร แก้คำค้าง จัดขอบขวา
5. **เรนเดอร์เป็นภาพแล้วดูจริง** ก่อนบอกว่าเสร็จ — อย่าส่งไฟล์ที่ยังไม่เคยเห็นหน้าตา
6. **ตรวจไฟล์** ว่าเป็น zip จริง ไม่มี marker ค้าง แล้วค่อยวางทับปลายทาง

## กฎที่ห้ามพลาด

### ขอบขวาต้องชิด แล้วแก้ช่องไฟด้วยการ "บีบ" ไม่ใช่เลิกจัดชิด

กิ๊ฟสอน 8 ก.ย. 2569: **"ยังต้องขยายให้ขอบขวาชิด แต่ต้องบีบบางส่วนหรือทั้งบรรทัด
มันเหมือนเป็นงานมนุษย์"**

เคยแก้ปัญหาช่องไฟถ่างด้วยการเลิกจัดชิดขอบขวา — **ผิด** วิธีที่ถูกคือคง `thaiDistribute` ไว้
แล้วบีบระยะห่างอักขระทีละขั้น จนดึงคำถัดไปขึ้นมาได้และช่องไฟเข้าเกณฑ์

ขั้นที่กิ๊ฟใช้จริง: **0.1 → 0.2 → 0.3 → 0.5 → 0.7 pt** (เกิน 0.7 เริ่มสังเกตออกด้วยตา)
เกณฑ์ผ่าน: ช่องว่างเฉลี่ยระหว่างอักขระ **ไม่เกิน 0.8 pt** วัดจาก PDF จริง

**บีบมากขึ้นไม่ได้ดีกว่าเสมอไป** — บรรทัดบนดึงคำขึ้นไปมาก บรรทัดที่รอก้อนคำยาวจะยิ่งสั้นและ
ถ่างหนักกว่าเดิม ต้องวัดทุกขั้นแล้ว **เลือกขั้นที่ถ่างน้อยที่สุด** ไม่ใช่ไล่บีบจนสุด 0.7

### คำค้างท้ายบรรทัดที่อ่านสะดุด ให้ย้ายลงไปพร้อมคำที่ตามมา

กิ๊ฟอธิบาย 17 ก.ย. 2569: **"มันก็ไม่ใช่คำบังคับที่ต้องขึ้นบรรทัดใหม่"**

ตัดสินด้วย **การอ่าน** ว่าความหมาย/รูปประโยคสะดุดไหม — **รายการคำในโค้ดเป็นแค่ตัวอย่างที่เจอจริง
ไม่ใช่กฎตายตัว** อย่าไล่เทียบรายการอย่างเดียว

หลัก: คำนำวลีห้ามตัด *หลัง* มัน แต่ตัด *ก่อน* ได้ · ถ้าคำถัดไปเป็นคำเชื่อมก็ตัดได้
กิ๊ฟโอเคให้เพิ่มกฎเองได้ ถ้าไม่ทำให้ความหมายเปลี่ยน ("เป็นลักษณะปรับถ้อย กิ๊ฟไม่ติดอะไร")

**กับดัก:** มัดก้อนยาวเกินไปแย่กว่าปล่อยคำค้าง — เคยมัด "มีวงเงินงบประมาณรวมทั้งสิ้น ๕๐,๐๓๘,๕๐๐ บาท)"
ซึ่งยาวเกือบครึ่งบรรทัด ทำให้บรรทัดบนถูกถ่างทั้งแถว กิ๊ฟเองยังปล่อยให้ตัดตรงนั้น

รายการคำและวิธีถอดกฎจากไฟล์ที่กิ๊ฟแก้มือ อยู่ใน `references/typesetting.md`

### ตัดบรรทัดเอง = line break ไม่ใช่ย่อหน้าใหม่

ที่กิ๊ฟกด **Alt+Enter** ต้องออกมาเป็น `<w:br/>` ในย่อหน้าเดิม
ถ้าเผลอขึ้นย่อหน้าใหม่ จะได้ space_before/after กับย่อหน้าบรรทัดแรกติดมาด้วย
บรรทัดที่ตัดจะเยื้องผิดและกินที่เพิ่ม

### ZWSP เฉพาะเนื้อความที่จัดชิดขอบขวา — และตัดคำจากทั้งย่อหน้า

Word ตัดบรรทัดไทย **ตรงไหนก็ได้ รวมทั้งกลางคำ** เพราะมันไล่เติมให้เต็มบรรทัด 99% โดยไม่สนขอบเขตคำ
วัดจากเอกสารจริงแล้วเจอตัดกลางคำ 12.7% ของบรรทัดที่ห่อ เช่น `ระบบสาก|ล` `กำหน|ด` `พื้น|ที่`

การตั้ง `w:lang w:bidi="th-TH"` **ไม่ช่วย** — ทดลองแยกตัวแปรแล้วผลเท่าเดิมทุกประการ
(ยังควรตั้งไว้เพราะเป็น metadata ที่ถูก แต่อย่าคาดหวังว่าจะแก้การตัดบรรทัด)

สิ่งที่ได้ผลคือ **ZWSP (U+200B)** — แต่ใส่ **เฉพาะเนื้อความที่จัดชิดขอบขวา** ไม่ใส่หัวเรื่อง ป้าย
บล็อกลายเซ็น หรือตาราง บทเรียนโม 09 (11 ก.ค. 2569): เคยแทรก ZWSP ทุกคำทุกย่อหน้า กิ๊ฟบ่นว่า
**"แยกถูกผิดไม่ออก"** โค้ดจริงปัจจุบันจึงเปิด ZWSP เฉพาะย่อหน้า `align="thai"`

ต้อง **ตัดคำจากข้อความทั้งย่อหน้า ไม่ใช่ทีละ run** เพราะเอกสารจาก Word มักมี run ที่ผ่ากลางคำ
(เจอจริง: `ประชาชนไม่โด` + `นทอดทิ้ง`) ถ้าตัดทีละ run จะได้ขอบเขตคำผิด
ใส่เฉพาะจุดที่ **ไทยชนไทย** จะได้ไม่ไปแหกคำอังกฤษหรือตัวเลข

แล้วต้องผ่าน **`keep_together`** เสมอ (ถอดจากโม 09) — ลบจุดตัดที่อ่านสะดุด และมัดคำด้วย NBSP:
"ความ"/"การ" ติดคำถัดไป · เลขกับหน่วย · จำนวนเงินกับ "บาท" · พ.ศ.กับปี · วงเล็บสั้น · เลขหนังสือ ·
คำนำวลี · ชื่อย่อ (สวรส. ม.มหิดล) · คำที่พจนานุกรมไม่มี · คำประสมที่ซอยแล้วความหมายเพี้ยน
รายละเอียดทุกข้อและวิธีเพิ่มคำอยู่ใน `references/typesetting.md`

### อ่านรอยต่อบรรทัดทุกจุดด้วยตาเอง

ตัวตรวจอัตโนมัติใช้พจนานุกรมเดียวกับตัวตัดคำ จึง **มองไม่เห็นคำที่พจนานุกรมไม่มี** —
ตอนทำตัวอย่างประเด็นถามตอบ จุดที่ผิดจริงทั้งหมด (`สว / รส.` · `เฝ้า / ระวัง` · `กรมควบคุม / โรค` ·
`(โทรศัพท์ / มือถือ/แท็บเล็ต)`) เจอจากการอ่านไล่ทีละจุด ไม่ใช่จากตัวตรวจ

`python scripts/inspect_docx.py ไฟล์.docx --breaks` แสดงรอยต่อทุกจุด อ่านว่าตรงไหนสะดุด
แล้วเพิ่มคำลง `DICT_GAPS` หรือ `WHOLE_COMPOUNDS` ใน `thai_break.py` — ไม่ต้องไปแก้มือในไฟล์

> **ระวังตอนเขียนโค้ด:** อย่าพิมพ์อักขระ ZWSP ลงไฟล์ตรง ๆ และอย่าเขียน escape ยูนิโค้ดผ่าน
> Edit/Write เพราะมันจะกลายเป็นตัวล่องหนจริงในซอร์ส ให้ใช้ `chr(0x200B)` แทนเสมอ
> แล้วสแกนไฟล์หาอักขระหมวด Cf/Cc หลังเขียน

### ระยะบรรทัด TH Sarabun ต่ำกว่า 1.0 ไม่ได้

วัดแล้ว: ที่ single (1.0) pitch = 18.14 pt ไม่มีบรรทัดซ้อนกัน
แต่ **0.95 เริ่มมีวรรณยุกต์ชนบรรทัดบน 4 จุด** · 0.92 และ 0.90 ชน 5 จุด

อยากได้ที่เพิ่ม ให้ไปลด `space_after` กับลบย่อหน้าว่างแทน
(ย่อหน้าว่าง 1 บรรทัดกิน ~26 pt ซึ่งคุมไม่ได้ ส่วน space_after คุมเป็น pt ได้)

### ตั้งฟอนต์ให้ครบฝั่ง complex script

python-docx ตั้ง `run.font.name` ให้แค่ `w:ascii`/`w:hAnsi` — อักษรไทยใช้ `w:cs`
ต้องตั้ง `w:cs` `w:szCs` `w:bCs` `w:iCs` ให้ครบทุก run ไม่งั้นบางเครื่องไทยตกไปใช้ 11 pt หรือไม่หนา

ลำดับลูกของ `w:rPr` ต้องตรงตาม schema (`rFonts` → `b` → `bCs` → … → `sz` → `szCs` → … → `lang`)
ถ้าเรียงผิด Word อาจทิ้ง property นั้นไปเงียบ ๆ

## กฎเหล็กของกิ๊ฟ (`กฎเหล็ก.md` ในรีโป)

1. **ตัวเลขต้นทางแตะไม่ได้** — งบบุคลากร ยอดคำขอ (BIS) เลขที่ผ่านการพิจารณาแล้ว
   อ่าน/คัดลอก/เทียบ/ชี้จุดไม่ตรงได้ แต่ **ห้ามแก้ไฟล์ต้นฉบับ** ต้องกิ๊ฟกดยืนยันเอง
   เครื่องมือเขียนได้แค่ไฟล์ผลลัพธ์ชุดใหม่ ต้นฉบับเปิด read-only เสมอ
2. **เจอกำกวมห้ามเดา** — ใช้รอบไหน วันที่เท่าไร บัญชี 1/2/3 ยุทธศาสตร์ไหน
   ต้องหยุดถามกิ๊ฟ ไม่ใช่เติมค่าปริยายเงียบ ๆ แล้วรายงานว่าผ่าน
3. **ทุกการเปลี่ยนมี log** — บันทึกลง `logs/audit.jsonl`: ไฟล์ต้นทาง + hash · เวลา · ทำอะไร · ผลเป็นอย่างไร

เส้นที่ห้ามข้าม: ห้ามขึ้น "ผ่าน" ทั้งที่ตรวจแค่ยอดรวม · ห้ามให้ AI ตัดสิน "อนุมัติ/ไม่อนุมัติ"
(ออกได้แค่ข้อสังเกต กิ๊ฟเป็นคนตัดสิน) · ห้ามเอาข้อมูลงานจริงไปปนกับงาน IS

## ยึดเกณฑ์ของปีปัจจุบัน

กิ๊ฟบอกไว้ (3 ก.ย. 2569): **"เรายึดตามปัจจุบันเลย ถ้าเกณฑ์มาแบบนี้เราทำตามเกณฑ์ตามปี"**

พอหนังสือเวียนแนวทางปีใหม่ออก ให้ถอดถ้อยคำ/กฎจากฉบับปีนั้นมาทั้งชุด
ไม่ใช่เอาของปีก่อนมาแก้เฉพาะเลขปี และไม่ต้องไล่เก็บตกส่วนที่ปีก่อนตกหล่น ถ้าปีใหม่ไม่มีเรื่องนั้นแล้ว

เอกสารที่อ้างเกณฑ์ผิดปีจะดูเหมือนถูกทุกอย่าง แต่ผิดที่สาระ — และตรวจด้วยตายาก

## โค้ดที่ใช้ได้เลย

อย่าเขียนตัวตัดคำหรือตัวตั้งฟอนต์ขึ้นใหม่ — ของพวกนี้มีแล้วและผ่านเทสต์จากจุดที่เคยพังจริง

```python
import sys; sys.path.insert(0, "<repo>/scripts")
from docx import Document
import thai_break, thai_fit

doc = Document("ต้นฉบับ.docx")
thai_fit.normalise_document(doc)          # ฟอนต์ครบฝั่ง cs + เคลียร์ docDefaults ที่ทำให้หน้ายืด
thai_fit.justify_body(doc)                # เนื้อความชิดขอบขวา (หัวเรื่อง/ป้าย/ลายเซ็นไม่โดน)
thai_break.insert_zwsp_document(doc)      # ZWSP + keep_together เฉพาะย่อหน้าที่ชิดขอบขวา
doc.save("ผลลัพธ์.docx")
```

ถ้าเอกสารจัดชิดขอบขวามาแล้ว ไม่ต้องเรียก `justify_body` — ถ้าไม่มีย่อหน้าไหนชิดขอบขวาเลย
`insert_zwsp_document` จะเตือน ไม่ใช่ไม่ทำอะไรเงียบ ๆ

```bash
python scripts/doctor.py --pdf                           # เครื่องใหม่: ตรวจความพร้อม + ลองส่งออก PDF จริง
python scripts/inspect_docx.py ผลลัพธ์.docx --png ภาพ   # วัดผลจริง + เรนเดอร์ภาพไว้ดู
python scripts/inspect_docx.py ผลลัพธ์.docx --breaks    # รอยต่อบรรทัดทุกจุด ไว้ไล่อ่าน
python tests/test_thai_break.py                          # รันก่อนแก้ตัวตัดคำทุกครั้ง
```

`thai_break` มี `insert_zwsp` (ข้อความเดี่ยว) · `insert_zwsp_paragraph` · `insert_zwsp_document` ·
`keep_together` · `plain_text` · `strip_zwsp` · `find_midword_breaks`
`thai_fit` มี `set_run_font` · `justify_body` · `set_char_spacing` · `pick_condense` · `line_break` · `set_line_spacing`
`inspect_docx` มี `to_pdf` · `midword_breaks` · `char_gaps` · `line_breaks` · `render_png` · `font_check` · `pdf_engine`

## เครื่อง Mac

ส่วนตัดคำและจัดหน้าเหมือนเดิมทุกอย่าง ต่างแค่ตอนส่งออก PDF เพื่อตรวจผล

- เรียก `python3` แทน `python` · เครื่องใหม่ให้รัน `python3 scripts/doctor.py --pdf` ก่อน
- `to_pdf` สั่ง Word for Mac ผ่าน AppleScript — เปิด **สำเนาชื่อสุ่ม** ใน `~/riabroi-pdf` แล้วปิดเฉพาะไฟล์นั้น
  **ห้ามสั่ง quit หรือ kill Word เอง และห้ามให้ AppleScript เปิดไฟล์จริงของผู้ใช้** — ถ้าไฟล์นั้นเปิดค้างอยู่
  Word จะคืนหน้าต่างเดิมมา แล้วการปิดแบบไม่บันทึกทำให้งานที่ยังไม่บันทึกหาย
- รายงานขึ้น "ไม่มี TH SarabunPSK ใน PDF" = เครื่องไม่มีฟอนต์ Word ใช้ตัวอื่นแทน — **หยุด** ผลวัดทั้งหมดใช้ไม่ได้
  ให้ผู้ใช้ติดตั้งฟอนต์ก่อน (Mac ไม่มี TH Sarabun มาให้)
- ขึ้นเรื่อง Automation หรือ Grant File Access = รอผู้ใช้กดอนุญาตที่หน้าจอ บอกผู้ใช้ อย่าวนรันซ้ำ
- **Word for Mac อาจตัดบรรทัดไม่ตรงกับ Windows** — เอกสารที่จะพิมพ์หรือส่งต่อจากเครื่อง Windows
  ต้องตรวจรอบสุดท้ายบน Windows
- PDF จาก LibreOffice (เครื่องที่ไม่มี Word) ใช้ดูภาพรวมได้ ห้ามใช้ตัดสินว่าผ่าน

รายละเอียดและที่มาของแต่ละข้ออยู่ใน `references/toolchain.md` หัวข้อ 3

## ไฟล์อ้างอิง

อ่านเมื่อถึงงานนั้นจริง ๆ ไม่ต้องอ่านหมดทุกครั้ง

| ไฟล์ | อ่านเมื่อ |
|---|---|
| `references/typesetting.md` | จัดหน้า บีบอักษร แก้คำค้าง ตั้งค่าย่อหน้า ลายเซ็น คำโปรย |
| `references/forms.md` | ทำใบเสนองาน ใบจัดสรร ประเด็นถามตอบ — ค่าขอบกระดาษและผังที่วัดไว้แล้ว |
| `references/toolchain.md` | เขียนโค้ดสร้างไฟล์ · Word COM · Word for Mac · อ่าน PDF แบบ สงป. · โค้ดที่มีอยู่แล้วในรีโปใหญ่ |

## ก่อนบอกว่าเสร็จ

ไล่ 7 ข้อนี้ทุกครั้ง:

- [ ] เทียบค่ากับแม่แบบแล้ว (ขอบกระดาษ ขนาดอักษร ตัวหนา ย่อหน้า)
- [ ] PDF ที่ใช้วัดมาจาก Word และฟอนต์ไม่ถูกแทน (ดูบรรทัด "ส่งออกด้วย" กับ "ฟอนต์ใน PDF" ในรายงาน)
- [ ] ZWSP อยู่เฉพาะเนื้อความ และไม่มีจุดตัดกลางคำเมื่อวัดจาก PDF ที่เรนเดอร์จริง
- [ ] **อ่านรอยต่อบรรทัดทุกจุดแล้ว** (`--breaks`) — ชื่อย่อ ชื่อหน่วยงาน คำประสม ไม่ขาด
- [ ] ขอบขวาชิด ช่องไฟเฉลี่ยไม่เกิน 0.8 pt
- [ ] เรนเดอร์เป็นภาพแล้วดูด้วยตาจริง ไม่ใช่เชื่อว่าโค้ดถูก
- [ ] ไฟล์ปลายทางเป็น zip เปิดได้ ไม่มี marker ค้าง

ถ้าเขียนไฟล์ปลายทางไม่ได้ (PermissionError) = กิ๊ฟเปิดค้างใน Word อยู่
**บอกกิ๊ฟให้ปิดก่อน ห้ามฆ่าโปรเซส Word ของกิ๊ฟ**
บน Mac ระบบไม่ล็อกไฟล์ที่เปิดใน Word จึงเขียนทับได้เงียบ ๆ ทั้งที่เปิดค้าง — ก่อนวางทับให้ถามก่อนว่าไฟล์ปลายทางเปิดอยู่ไหม
